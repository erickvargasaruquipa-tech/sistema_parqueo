Sistema Parqueo - Proyecto Flask (SQLite)
=========================================
Instrucciones rápidas:
1) Crear y activar un entorno virtual (recomendado)
   python -m venv venv
   source venv/bin/activate   (Linux/mac) or venv\Scripts\activate (Windows)
2) Instalar dependencias:
   pip install -r requirements.txt
3) Ejecutar:
   python app.py
4) Usuario superadmin ya creado:
   usuario: maruja
   contraseña: maru445v

Estructura básica: models, controllers (blueprints), templates, static.
